<%= form_for(@listing_task) do |f| %>
  <% if @listing_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@listing_task.errors.count, "error") %> prohibited this listing_task from being saved:</h2>

      <ul>
      <% @listing_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
